fastapi==0.95.2
uvicorn==0.23.2
openai==0.28.0
python-dotenv==1.0.0
pydantic==1.10.8
httpx==0.24.1
python-multipart==0.0.6

# Librerie per lo sviluppo e la manutenzione
black==23.7.0     # Formattazione del codice
isort==5.12.0     # Ordinamento degli import
python-dotenv==1.0.0

# Dipendenze per la gestione delle immagini
Pillow==10.0.0    # Modifica delle immagini
uuid==1.30        # Generazione nomi univoci per file
